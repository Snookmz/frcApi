package handler


func (h *Handler) SaveScoutsCsvToDisk (scouts []Scout) (err error) {
	//
	//w := csv.NewWriter(os.Stdout)
	//
	//for _, obj := range scouts {
	//	var record []string
	//	record = append(record, strconv.FormatInt(obj.RecordID, 10))
	//	record = append(record, obj.DOJ)
	//	record = append(record, obj.EmpID)
	//	w.Write(record)
	//}
	//
	////v := reflect.ValueOf(scouts)
	////values := make([]interface{}, v.NumField())
	////
	////for i := 0; i < v.NumField(); i++ {
	////	values[i] = v.Field(i).Interface()
	////}
	////
	////fmt.Println(values)
	//
	return
}
